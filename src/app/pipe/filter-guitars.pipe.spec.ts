import { FilterGuitarsPipe } from './filter-guitars.pipe';

describe('FilterGuitarsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterGuitarsPipe();
    expect(pipe).toBeTruthy();
  });
});
